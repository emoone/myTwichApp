import { useState } from 'react';
import getTwitchDetailData from '../../api/getTwitchDetailData';

const GetGameList = () => {
  const [gameList, setGameList] = useState([]);
  // const getGamesData = getTwitchDetailData(gameName)

  // const getData = async () => {};
};

export default GetGameList;
